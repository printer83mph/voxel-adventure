{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build (Debug)",
			"type": "shell",
			"command": "cmake",
			"args": [
				"--build",
				".",
				"--target=voxel-editor",
				"--config=Debug",
				"--",
				"-j", "8"
			],
			"options": {
				"cwd": "${workspaceFolder}/build"
			},
			"problemMatcher": [
				"$gcc"
			]
		}
	]
}